version: '3.8'
services:
  web-dev:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install -g yarn && yarn install && yarn dev"

